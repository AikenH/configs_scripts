-- load the basic config file.
require("basic.settings")
require("basic.plugins")
require("basic.keybinds")
require("basic.config")

-- we need to add the DAP, running, and add header or snippet for out diff files.
-- support the cliptboard sync and the ctrl-A or some keymaps good.
-- find out more themes for nvim 
-- Fix the Problems of those plugins.

